source ~/.profile
[[ -s "$HOME/.rvm/scripts/rvm" ]] && \ 
. "$HOME/.rvm/scripts/rvm"
[[ -s "$HOME/.rvm/scripts/rvm" ]] && \
. "$HOME/.rvm/scripts/rvm"
if [ -f /etc/bash_completion ]; then
     . /etc/bash_completion 
fi
